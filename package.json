{
  "name": "sparqlist",
  "version": "0.0.0",
  "main": "index.js",
  "repository": {},
  "dependencies": {
    "accepts": "^1.3.3",
    "body-parser": "^1.16.0",
    "express": "^4.14.0",
    "express-promise-router": "^1.1.1",
    "form-data": "^2.1.2",
    "fs-promise": "^1.0.0",
    "handlebars": "^4.0.6",
    "lodash": "^4.17.4",
    "marked": "0.3.12",
    "morgan": "^1.7.0",
    "node-fetch": "^2.0.0",
    "type-is": "^1.6.14"
  },
  "scripts": {
    "start": "node index.js",
    "build": "cd frontend && yarn install --production=false && yarn build --environment=production --output-path=../public",
    "postinstall": "yarn build",
    "watch": "nodemon --exec yarn start --ignore frontend/"
  },
  "engines": {
    "node": "8.x"
  },
  "private": true,
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
