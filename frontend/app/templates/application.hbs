<nav class="navbar navbar-toggleable-xl navbar-inverse bg-inverse fixed-top">
<a class="navbar-brand" href="/">SPARQList</a>
<ul class="navbar-nav">
  <li class="nav-item">
    {{#if session.isAuthenticated}}
      <a {{action 'invalidateSession'}} class="nav-link">Logout</a>
    {{else}}
      {{#link-to 'login' class="nav-link"}}Login{{/link-to}}
    {{/if}}
  </li>
</ul>
</nav>

<div class="container">
  {{outlet}}
</div>
