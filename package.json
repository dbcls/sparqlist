{
  "name": "sparqlist",
  "version": "0.0.0",
  "main": "index.js",
  "repository": {},
  "dependencies": {
    "body-parser": "^1.16.0",
    "express": "^4.14.0",
    "express-promise-router": "^1.1.1",
    "form-data": "^2.1.2",
    "fs-promise": "^1.0.0",
    "handlebars": "^4.0.6",
    "lodash": "^4.17.4",
    "marked": "^0.3.6",
    "morgan": "^1.7.0",
    "node-fetch": "^1.6.3"
  },
  "scripts": {
    "start": "node --harmony-async-await index.js",
    "build": "cd frontend && NPM_CONFIG_PRODUCTION=false yarn install --prefer-offline && yarn build -- --environment=production --output-path=../public",
    "postinstall": "yarn build",
    "watch": "nodemon --exec yarn start --ignore frontend/"
  },
  "engines": {
    "node": "7.5.0"
  },
  "private": true,
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
