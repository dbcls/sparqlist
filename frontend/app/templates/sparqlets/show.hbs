{{#each error.errors as |error|}}
<div class="alert alert-danger">
  <strong>Error</strong> {{error.detail}}
</div>
{{/each}}

{{sparqlet-runner title=model.title apiPath=model.apiPath traceModeApiPath=model.traceModeApiPath params=model.params}}

<div class="card" style="margin-top: 10px;">
  <div class="card-header">
    <strong>API code document</strong>
    {{#if session.isAuthenticated}}
      <button {{action 'delete' model}} class="btn btn-outline-danger float-right">{{fa-icon 'trash'}} Delete</button>
      <p class="float-right">&nbsp;</p>
      {{#link-to 'sparqlets.edit' model class="btn btn-outline-primary float-right" role="button"}}{{fa-icon 'edit'}} Edit{{/link-to}}
      <p class="float-right">&nbsp;</p>
      <button {{action 'fork' model}} class="btn btn-outline-primary float-right">{{fa-icon 'code-fork'}} Fork</button>
    {{/if}}
    <div>{{model.mdPath}}</div>
  </div>
  <div class="card-block">
    {{{model.html}}}
  </div>
</div>
